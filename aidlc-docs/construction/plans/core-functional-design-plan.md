# Unit 1: core — 機能設計計画

コアエンジンの詳細なビジネスロジックを確定するための質問です。
各 `[Answer]:` タグに回答を記入して「完了しました」とお知らせください。

---

## 質問 1 — ダウンロードファイルの命名規則

オーディオファイルを保存するときのファイル名はどうしますか？

A) RSSフィードの enclosure URL のファイル名をそのまま使う（例: `episode-123.mp3`）

B) エピソードタイトルをサニタイズしてファイル名にする（例: `2024-06-21_タイトル.mp3`）

C) フィードラベル + 日付 + タイトルの組み合わせ（例: `MyPodcast_20240621_タイトル.mp3`）

D) Other (以下に記述してください)

[Answer]: C

---

## 質問 2 — 部分ダウンロード（中断後の再開）

ダウンロードが途中で中断された場合の扱いはどうしますか？

A) 中断ファイルを削除して最初からダウンロードし直す（シンプル）

B) 中断したところから再開する（HTTP Range リクエストを使用）

C) Other (以下に記述してください)

[Answer]: A

---

## 質問 3 — エピソードキャッシュの更新タイミング

登録済みフィードのエピソードキャッシュはいつ更新しますか？

A) ユーザーが手動で「更新」ボタンを押したときのみ

B) アプリ起動時に自動取得する（バックグラウンドで静かに）

C) A と B の両方（起動時自動 + 手動更新）

D) Other (以下に記述してください)

[Answer]: C

---

## 質問 4 — RSSフィールドが欠損している場合の扱い

RSS フィードによっては一部フィールドが存在しないことがあります。欠損時の方針を選んでください。

A) 欠損フィールドは空文字または `None` で表示し、そのエピソードも一覧に表示する

B) 必須フィールド（タイトル・音声URL）が欠損したエピソードは一覧から除外する

C) A を基本とし、音声URL が欠損したエピソードのみ除外する

D) Other (以下に記述してください)

[Answer]: C

---

## 質問 5 — ダウンロード済みファイルの重複チェック

すでにダウンロード済みのファイルがある場合の動作はどうしますか？

A) キャッシュのステータス（`DOWNLOADED`）を確認し、ファイルも実際に存在すれば「済」と表示してスキップ

B) ファイルの存在確認のみ（キャッシュは使わず、毎回ファイルシステムを検索）

C) ユーザーが常に上書きダウンロードできる選択肢を提供する（スキップしない）

D) Other (以下に記述してください)

[Answer]: B

---

## 質問 6 — ネットワークエラー時のリトライ

RSS取得やダウンロード中にネットワークエラーが発生した場合のリトライ動作はどうしますか？

A) リトライなし — エラーをすぐにユーザーに通知する

B) 自動リトライあり — 最大3回、指数バックオフ（1秒→2秒→4秒）でリトライ後にエラー通知

C) Other (以下に記述してください)

[Answer]: A

---

## 生成チェックリスト

- [x] `business-logic-model.md` — ビジネスロジックモデル
- [x] `business-rules.md` — ビジネスルール・バリデーション
- [x] `domain-entities.md` — ドメインエンティティ詳細
